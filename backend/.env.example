# Server Configuration
PORT=3000
NODE_ENV=development

# Database Configuration
# Use ':memory:' for in-memory database (demo mode)
# Use path for persistent storage (e.g., '../bot-manager.db')
DB_PATH=../bot-manager.db

# JWT Secret (generate with: openssl rand -base64 32)
JWT_SECRET=your-secret-key-here

# CORS Configuration
# Comma-separated list of allowed origins
ALLOWED_ORIGINS=http://localhost:5173,http://localhost:3000,https://your-production-domain.com

# Logging Configuration
LOG_LEVEL=info
# Available levels: error, warn, info, http, verbose, debug, silly

# Security Configuration
# Rate limiting is enabled by default
# Helmet security headers are enabled by default

# PM2 Configuration
# PM2 should be installed globally: npm install -g pm2

# Production Recommendations:
# - Set NODE_ENV=production
# - Use a strong JWT_SECRET (min 32 characters)
# - Set LOG_LEVEL=warn or error
# - Configure ALLOWED_ORIGINS to only include your production domains
# - Use HTTPS in production
# - Keep DB_PATH secure and backed up regularly
